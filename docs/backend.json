
{
  "entities": {
    "UserProfile": {
      "title": "User Profile",
      "description": "Stores user game stats, balance, and Telegram profile information.",
      "type": "object",
      "properties": {
        "uid": { "type": "string", "description": "Unique identifier for the user (CNXXXXXX format)." },
        "username": { "type": "string" },
        "telegramId": { "type": "string" },
        "avatarUrl": { "type": "string" },
        "coins": { "type": "number" },
        "xp": { "type": "number" },
        "level": { "type": "number" },
        "adsWatched": { "type": "number" },
        "tasksCompleted": { "type": "number" },
        "lastWithdrawalAt": { "type": "number" },
        "tier": { "type": "string", "enum": ["Silver", "Gold", "Diamond", "God Elite"] },
        "joinedAt": { "type": "number" },
        "referralCode": { "type": "string" },
        "referredBy": { "type": "string", "description": "UID of the user who invited this user." },
        "referralEarnings": { "type": "number" },
        "referrals": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "uid": { "type": "string" },
              "username": { "type": "string" },
              "adsWatched": { "type": "number" },
              "joinedAt": { "type": "number" },
              "isRewarded": { "type": "boolean" }
            }
          }
        }
      },
      "required": ["uid", "username", "coins", "referralCode"]
    }
  },
  "auth": {
    "providers": ["anonymous"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "#/backend/entities/UserProfile",
      "description": "User profile document indexed by Firebase Auth UID."
    }
  }
}
